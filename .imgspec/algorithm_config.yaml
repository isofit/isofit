# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algo_name: isofit
version: v2.9.1
environment: ubuntu
repository_url: https://github.com/isofit/isofit.git
docker_url: registry.imgspec.org/root/ade_base_images/isofit:latest

# fill out these fields
# explain what this algorithm does
description: Imaging Spectrometer Optimal FITting (ISOFIT) contains a set of routines and utilities for fitting
  surface, atmosphere and instrument models to imaging spectrometer data.
# path to the wrapper script for running the algorithm
run_command: isofit/.imgspec/imgspec_run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 70GB
inputs:
# remove below this line if no inputs
# rename and set algorithm input names accordingly
  - name: l1_granule
    type: file
  - name: surface_reflectance_spectra
    type: positional
  - name: vegetation_reflectance_spectra
    type: positional
  - name: water_reflectance_spectra
    type: positional
  - name: snow_and_liquids_reflectance_spectra
    type: positional
  - name: segmentation_size
    type: positional
    default: 200
  - name: n_cores
    type: positional
    default: 32
  - name: radiance_factors_file
    type: positional
    default: None
